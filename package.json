{
  "name": "tetris-game",
  "version": "1.0.0",
  "description": "Browser-based Tetris game with Playwright tests",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "serve": "npx http-server . -p 3000 -c-1"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "http-server": "^14.1.1"
  }
}
